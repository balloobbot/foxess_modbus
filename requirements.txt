# The minimum supported version is the 2026.5.0 declared in hacs.json, which is where
# SerialPortSelector landed (and where HA started requiring Python 3.14). Tests run against
# the latest release, so breakage on what most users are running shows up here first.
homeassistant==2026.8.0

# Testing
pytest-homeassistant-custom-component==0.13.354  # Matching version
psutil-home-assistant  # Not sure why this is needed?
fnv_hash_fast  # Or this?
pytest-asyncio

# For pytest. Keep in sync with manifest.json.
modbus-connection[tmodbus]>=4.6.0

# Tooling
colorlog==6.10.1
pre-commit==4.6.0
ruff==0.9.4
mypy==1.15.0

# Typing
homeassistant-stubs==2026.8.0 # Matching HA version
types-python-slugify==8.0.2.20240310
voluptuous-stubs==0.1.1
